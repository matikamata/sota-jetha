# SĀRA·SUDDHI / Sotā Public Landing Draft

Static public landing/demo draft for a cautious Jetha-facing preview.

This directory is intentionally separate from the protected reviewer tool and contains only public-safe landing copy plus public YouTube embeds/links.

It includes:

- `index.html`
- `styles.css`
- `app.js`
- `README.md`

It does not include protected operational artifacts, media files, credentials, or local machine paths.

## Local preview

```bash
python3 -m http.server 8787
```

Then open:

```text
http://127.0.0.1:8787/
```

## Status

Draft only. Do not push, deploy, publish, or send without explicit human approval.
